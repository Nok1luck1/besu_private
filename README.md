# besu_private
chmod +x generate-keys.sh 
run ./generate_keys.sh node1 for generation 1 node
chmod +x start_blockchain.sh for start_blockchain
 generate address of validators need to be stored in /validator-smart-contract/scripts/GenesisContent allowedAccount 
 1 address must be duplicated twice for each validator with their address
 after that copy compiled date into storage inside genesis.json

